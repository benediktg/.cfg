[user]
    name = Benedikt Geißler
    email = benedikt@g5r.eu
    signingkey = 8A46815E3386097F
[push]
    default = simple
[credential]
    helper = cache --timeout=7200
[core]
    excludesfile = ~/.gitignore_global
[color]
    ui = auto
[pack]
    threads = 2
    windowMemory = 1073741824
    depth = 250
    window = 250
[url "git://anongit.kde.org/"]
    insteadOf = kde:
[url "git@git.kde.org:"]
    pushInsteadOf = kde:
[url "https://gitlab.com/"]
    insteadOf = gl:
[url "https://github.com/"]
    insteadOf = gh:
[diff]
    guitool = kompare
[alias]
    master = checkout master
    up = !git fetch && git rebase origin/master
    pl = pull --rebase
    ps = push
    pf = push --force-with-lease
    pso = push origin :
    m = merge --no-ff
    ma = merge --abort
    mt = mergetool
    mtt = mergetool --tool
    st = status
    sh = stash
    ai = add --interactive
    ap = add --patch
    d = diff
    dc = diff --cached
    dp = diff --patch --patience
    dh = diff HEAD~ HEAD
    r = remote
    rv = remote --verbose
    cv = commit --verbose
    cva = commit --verbose --all
    ca = commit --all
    co = checkout
    cob = checkout -b
    cp = cherry-pick
    l = log
    lp = log --patch
    b = branch
    reha = reset --hard
    lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%Creset' --abbrev-commit --date=relative
    rb = rebase
    rbo = rebase --onto=master
    rbs = rebase --skip
    rbc = rebase --continue
    rbi = rebase --interactive
    rba = rebase --abort
    ui = gui
    nm = config --global user.name
    em = config --global user.email
    nml = config user.name
    eml = config user.email
    currentbranch = rev-parse --abbrev-ref HEAD
    logn = !git lg | head -n
    k = !gitk
    ka = !gitk --all
    t = !tig
    ta = !tig --all
    f = fetch
    s = show
    sb = show-branch
    fp = fetch --prune
    cg = config --global
    cf = config
	undo = "!f() { \\\n    git reset --hard $(git rev-parse --abbrev-ref HEAD)@{${1-1}}; \\\n}; f"
[gui]
    fontui = -family Oxygen-Sans -size 10 -weight normal -slant roman -underline 0 -overstrike 0
    spellingdictionary = en
    tabsize = 4
    fontdiff = -family Hack -size 10 -weight normal -slant roman -underline 0 -overstrike 0
[merge]
    tool = kdiff3
[pull]
    ff = only
