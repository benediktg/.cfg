abbr -a -U -- c 'git --git-dir=$HOME/.cfg --work-tree=$HOME'
abbr -a -U -- cdt 'cd (mktemp -d)'
abbr -a -U -- check 'config status -u --'
abbr -a -U -- config 'git --git-dir=$HOME/.cfg --work-tree=$HOME'
abbr -a -U -- cow 'cp -a --reflink=always'
abbr -a -U -- docker podman
abbr -a -U -- g git
abbr -a -U -- gi git
abbr -a -U -- mkdir 'mkdir -p'
abbr -a -U -- userctl 'systemctl --user'
abbr -a -U -- vi vim
